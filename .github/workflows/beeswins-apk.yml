name: Build BeesWins APK

on:
  pull_request:
    branches: [main]
    paths:
      - 'beeswins-v1.1/**'
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  build:
    if: github.event_name != 'issues' || contains(github.event.issue.title, '[BUILD BEESWINS]')
    name: Build BeesWins v1.1.1
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout BeesWins v1.1.1 source
        id: source
        uses: actions/checkout@v4
        with:
          ref: claude/beeswins-v1.1.1-date-picker

      - name: Verify source
        shell: bash
        run: |
          test -f beeswins-v1.1/BeesWins/settings.gradle
          test -f beeswins-v1.1/BeesWins/app/src/main/java/za/co/beeswins/MainActivity.java
          test -f beeswins-v1.1/BeesWins/app/src/main/java/za/co/beeswins/BeesWinsApp.java
          test -f beeswins-v1.1/BeesWins/app/src/main/res/drawable/beeswins_logo.xml

      - name: Set up Java 17
        id: java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.11.1
        id: gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Install Android SDK 35
        id: sdk
        shell: bash
        run: |
          set -o pipefail
          {
            SDKMANAGER="${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager"
            if [ ! -x "$SDKMANAGER" ]; then
              SDKMANAGER="$(command -v sdkmanager)"
            fi
            echo "sdkmanager=$SDKMANAGER"
            yes | "$SDKMANAGER" --licenses >/dev/null || true
            "$SDKMANAGER" 'platforms;android-35' 'build-tools;35.0.0'
          } 2>&1 | tee /tmp/beeswins-sdk.log

      - name: Build installable APK
        id: compile
        working-directory: beeswins-v1.1/BeesWins
        shell: bash
        run: |
          set -o pipefail
          gradle :app:assembleDebug --no-daemon --stacktrace 2>&1 | tee /tmp/beeswins-gradle.log

      - name: Prepare APK
        id: prepare
        run: |
          mkdir -p dist
          cp beeswins-v1.1/BeesWins/app/build/outputs/apk/debug/app-debug.apk dist/BeesWins-v1.1.1.apk
          sha256sum dist/BeesWins-v1.1.1.apk > dist/BeesWins-v1.1.1.apk.sha256
          ls -lh dist/

      - name: Upload GitHub Artifact
        id: artifact
        uses: actions/upload-artifact@v4
        with:
          name: BeesWins-v1.1.1-APK
          path: |
            dist/BeesWins-v1.1.1.apk
            dist/BeesWins-v1.1.1.apk.sha256
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

      - name: Publish APK to isolated output branch
        id: publish
        shell: bash
        run: |
          cp dist/BeesWins-v1.1.1.apk /tmp/BeesWins-v1.1.1.apk
          cp dist/BeesWins-v1.1.1.apk.sha256 /tmp/BeesWins-v1.1.1.apk.sha256
          git fetch origin claude/beeswins-gas-output
          git switch claude/beeswins-gas-output
          mkdir -p beeswins-output
          cp /tmp/BeesWins-v1.1.1.apk beeswins-output/BeesWins-v1.1.1.apk
          cp /tmp/BeesWins-v1.1.1.apk.sha256 beeswins-output/BeesWins-v1.1.1.apk.sha256
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add beeswins-output/BeesWins-v1.1.1.apk beeswins-output/BeesWins-v1.1.1.apk.sha256
          if git diff --cached --quiet; then
            echo 'APK output already current.'
          else
            git commit -m 'Publish BeesWins v1.1.1 APK [skip ci]'
            git push origin HEAD:claude/beeswins-gas-output
          fi

      - name: Report issue-trigger result
        if: always() && github.event_name == 'issues'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          SOURCE_OUTCOME: ${{ steps.source.outcome }}
          SDK_OUTCOME: ${{ steps.sdk.outcome }}
          COMPILE_OUTCOME: ${{ steps.compile.outcome }}
          PREPARE_OUTCOME: ${{ steps.prepare.outcome }}
          ARTIFACT_OUTCOME: ${{ steps.artifact.outcome }}
          PUBLISH_OUTCOME: ${{ steps.publish.outcome }}
        run: |
          {
            echo '### BeesWins v1.1.1 build result'
            echo
            echo "- Source: ${SOURCE_OUTCOME:-not-run}"
            echo "- Android SDK: ${SDK_OUTCOME:-not-run}"
            echo "- Compile: ${COMPILE_OUTCOME:-not-run}"
            echo "- Prepare APK: ${PREPARE_OUTCOME:-not-run}"
            echo "- GitHub Artifact: ${ARTIFACT_OUTCOME:-not-run}"
            echo "- Publish output: ${PUBLISH_OUTCOME:-not-run}"
            echo
            echo '<details><summary>Last runner log lines</summary>'
            echo
            echo '```text'
            for f in /tmp/beeswins-sdk.log /tmp/beeswins-gradle.log; do
              if [ -f "$f" ]; then
                echo "===== $(basename "$f") ====="
                tail -n 55 "$f"
              fi
            done
            echo '```'
            echo '</details>'
          } > /tmp/beeswins-report.md
          gh issue comment "$ISSUE_NUMBER" --body-file /tmp/beeswins-report.md
